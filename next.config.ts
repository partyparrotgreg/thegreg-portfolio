import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["localhost", "images.unsplash.com", "www.datocms-assets.com"],
  },
};

export default nextConfig;
